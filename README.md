# risk-cards

Risk cards

## Install

```bash
npm install risk-cards
```

## Cards

A card is like

```js
{
  continent: "europe",
  region: "southern-europe",
  type: "cavalier",
}
```

Possible card's types: `cannon`, `cavalier`, and `soldier`.
